package com.clientes.clientes_app;

import java.text.SimpleDateFormat;
import java.util.Date;

public class App {
    public static void main(String[] args) {
        ClienteService clienteService = new ClienteService();
/*
        // Crear un cliente
        try {
            SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd");
            Date fechaNacimiento = sdf.parse("1990-05-15");

            Cliente cliente1 = new Cliente(
                    "Juan", "Pérez", "Gómez", "C123", "X987654", "Calle Falsa", "10", "5", "B", 
                    "28080", "Madrid", "Madrid", "12345678A", fechaNacimiento
            );

            // Crear el cliente y guardarlo en el archivo JSON
            clienteService.crearCliente(cliente1);

            // Ver los clientes almacenados
            System.out.println(clienteService.leerClientes());
        } catch (Exception e) {
            e.printStackTrace();
        }
        */
    }
}
